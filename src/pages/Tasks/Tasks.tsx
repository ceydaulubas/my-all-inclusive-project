import React from 'react';
import { WeatherDisplay } from '../../components/index';

function Tasks() {
    return (
        <div className="Tasks" style={{ marginTop: 80 }}>
            Tasks
        </div>
    );
}

export default Tasks;
