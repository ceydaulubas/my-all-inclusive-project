import styled from 'styled-components';
import { LoadingOutlined } from '@ant-design/icons';

export const StyledSpinner = styled(LoadingOutlined)`
    font-size: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    height: 60%;
    margin: auto;
    color: 'darkblue';
`;
